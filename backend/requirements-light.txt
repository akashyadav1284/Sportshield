# ─── Web framework ───
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# ─── Database ───
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1

# ─── Celery + Redis ───
celery[redis]==5.3.6
redis>=4.5.2,<5.0.0,!=4.5.5
celery-redbeat==2.2.0

# ─── Auth ───
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─── HTTP client ───
httpx==0.27.0

# ─── Image processing (lightweight) ───
Pillow==10.2.0
imagehash==4.3.1

# ─── Email ───
sendgrid==6.11.0

# ─── WebSocket ───
python-socketio==5.11.1

# ─── Storage ───
boto3==1.34.34

# ─── PDF ───
reportlab==4.1.0

# ─── Rate limiting & Caching ───
slowapi==0.1.9
fastapi-cache2[redis]==0.2.1

# ─── Utilities ───
pydantic[email]==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1
numpy==1.26.4

# ─── NOTE ───
# This file EXCLUDES heavy AI/ML dependencies for free-tier deployment:
#   - torch, torchvision (~2GB)
#   - faiss-cpu
#   - transformers, huggingface-hub
#   - opencv-python-headless
#   - ffmpeg-python
#   - python-magic
#   - google-api-python-client
# The web app (login, dashboard, Shield AI, violations, takedowns) works
# fully without these. Only the background AI scanning pipeline needs them.
